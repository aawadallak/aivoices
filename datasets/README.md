# Datasets

Processed outputs live here.

Suggested structure:

- `datasets/<series>/clips/`: final audio clips
- `datasets/<series>/splits/`: train, validation, and test manifests
- `datasets/<series>/exports/`: packaged outputs for training or sharing

Keep this directory reproducible from `episodes/`, `metadata/`, and `scripts/` whenever possible.
