# silvio-santos manual-import

The imported package currently lives under `output/` with:

- `metadata_train.csv`
- `metadata_eval.csv`
- `wavs/`

Note: the imported package has been normalized to use `speaker_name = silvio-santos`.

If we later build our own dataset from raw media, keep that flow separate and publish normalized exports under `datasets/silvio-santos/exports/`.
