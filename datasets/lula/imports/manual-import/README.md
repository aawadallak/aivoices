# lula manual-import

The imported package currently lives under `output/` with:

- `metadata_train.csv`
- `metadata_eval.csv`
- `wavs/`

If we later build our own dataset from raw media, keep that flow separate and publish normalized exports under `datasets/lula/exports/`.
